#include <stdio.h>
#include <stdlib.h>

int main()
{       
        printf("hwllo");
        // int a = 10, b=20,c=1;
        // printf("%d %d ",a,b,c);// %d eksik olursa hata vermez c değerini göstermez

        // int a = 10, b=20,c;
        // printf("%d %d ",a,b,c);//eğer int c değeri atarsam ve tanımlamazsam bunla birlikte %d ile çağırmazsam kod çalışır ve c değeri göstermez hata vermez

        // int a = 10, b=20,c;
        // printf("%d %d %d",a,b,c);//eğer int c değeri atarsam ve c yi tanımlamazsam rastgele c değeri çıkrisi verir hata vermez
        // int a = 10, b=20;
        // printf("%d %d %d",a,b,c); eğer int c değeri tanımlamazsam hataverir çalışmaz 

        //char isim[7]= "abdullah";// \0 hesaba katmaksak \0 7 inci karakter olur yani hata verir abulla diye çıktı alır     
        //printf("%s", isim);
         return 0;
}